{
  "name": "agntpymt",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "client",
    "server",
    "db"
  ],
  "scripts": {
    "dev": "npm run build -w db && node scripts/free-port.mjs && concurrently -n client,server -c cyan,violet \"npm run dev -w client\" \"npm run dev -w server\"",
    "dev:all": "npm run build -w db && node scripts/free-port.mjs && concurrently -n client,server,hermes -c cyan,violet,green \"npm run dev -w client\" \"npm run dev -w server\" \"node scripts/start-hermes.mjs\"",
    "dev:hermes": "node scripts/start-hermes.mjs",
    "build": "npm run build -w db && npm run build -w server && npm run build -w client",
    "docker:build": "docker build -t agntpymt:local .",
    "docker:run": "docker run --rm -p 8080:8080 -e DATABASE_URL=postgresql://host.docker.internal:5432/agntpymt -e POSTGRES_HOST=host.docker.internal -e SIMULATE_PAYMENTS=true agntpymt:local",
    "db:up": "docker compose -f deploy/docker/docker-compose.postgres.yml up -d",
    "db:down": "docker compose -f deploy/docker/docker-compose.postgres.yml down",
    "db:ensure": "npm run ensure -w db",
    "gcp:bootstrap": "bash deploy/gcp/bootstrap.sh",
    "start:prod": "npm run db:migrate && npm run db:seed && npm run start -w server",
    "db:generate": "npm run generate -w db",
    "db:migrate": "npm run migrate -w db",
    "db:seed": "npm run seed -w db"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "typescript": "^5.7.3"
  }
}
